# The API the client talks to. Without this it falls back to the local dev
# server at http://localhost:5001/api/v1, which is correct for development but
# would break a deployed build.
VITE_API_URL=http://localhost:5001/api/v1

# Interface language on first load: ar (default) or en.
VITE_DEFAULT_LOCALE=ar
