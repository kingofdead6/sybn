// Products seed data — section 7.12
// No real products exist yet: the store should ship with a proper empty
// state rather than fake/dead entries. Intentionally empty.

const products = [];

export default products;
